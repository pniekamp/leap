language: cpp

matrix:
  include:
    # Clang 5.0
    - env: COMPILER=clang++-5.0 CMAKE_OPTIONS=""
      addons: { apt: { packages: ["g++-5", "clang-5.0"], sources: ["ubuntu-toolchain-r-test", "llvm-toolchain-trusty-5.0"] } }

install:
  - export CXX=${COMPILER}
  - ${CXX} --version
  - cmake --version

before_script:
  - mkdir build
  - cd build
  - cmake ${CMAKE_OPTIONS} ..

script: 
  - make
